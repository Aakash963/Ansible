---
- hosts: 172.31.6.127
  become: yes 
  tasks:
  - name: i want to execute linux command
    command: hostname -f
    register: mytaks


  - name: i want display my fisrt tasks result
    debug:
    msg: "{{mytaks}}" 